@model OtelEntity

<div class="row">
    <div class="col">
        <div class="card" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">@Model.Adi</h5>
                <h6 class="card-subtitle mb-2 text-body-secondary">@Model.Yildizi</h6>
                <p class="card-text">@Model.Turu</p>

                <form asp-controller="Otel" asp-action="DeleteConfirm">
                    <input type="hidden" name="Id" value="@Model.Id" />
                    <input type="submit" value="Sil" onclick="alert('Hotel Silindi!')" class="btn btn-danger" />
                </form>

            </div>
        </div>
    </div>
</div>
