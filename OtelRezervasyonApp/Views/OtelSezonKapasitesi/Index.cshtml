@model IEnumerable<OtelRezervasyonApp.Data.Entities.OtelSezonKapasitesi>

@{
    ViewData["Title"] = "Index";
}

<h4>Otel Sezon Kapasitesi</h4>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Otel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sezon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OtelSezonKapasite)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Otel.Adi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sezon.SezonAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OtelSezonKapasite)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
